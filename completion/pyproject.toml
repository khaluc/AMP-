[project]
name = "amp-completion"
version = "0.1.0"
description = "Reproduce and audit the existing AMP research pipeline"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
generate = "amp_completion.pipeline:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
